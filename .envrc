#!/usr/bin/env bash

export DIRENV_WARN_TIMEOUT=999s
VENV_ACTIVATE_SCRIPT=".devenv/state/venv/bin/activate"

eval "$(devenv direnvrc)"

use devenv

# shellcheck source=.devenv/state/venv/bin/activate
if [ -s "$VENV_ACTIVATE_SCRIPT" ]; then
  source "$VENV_ACTIVATE_SCRIPT"
fi
